{
  "schema_version": "1.0.0",
  "last_updated": "2026-07-07",
  "source_docs": [
    "openai-codex-app-automations",
    "openai-codex-app-worktrees",
    "openai-codex-best-practices",
    "openai-codex-sandbox-defaults",
    "openai-codex-record-and-replay"
  ],
  "automation_contracts": [
    {
      "id": "report-only-official-docs-refresh",
      "owner": "agent-dev-kit",
      "mode": "report-only",
      "enabled_default": false,
      "prompt_requirements": [
        "durable task description",
        "bounded data sources",
        "cadence",
        "stop condition",
        "review destination"
      ],
      "run_context": "standalone",
      "sandbox_policy": "workspace-write + on-request unless explicitly reviewed",
      "approval_policy": "human review before file writes, commits, pushes, publishes, deletes or external notifications",
      "first_run_review": true,
      "first_run_evidence": [
        "small-scope real run or explicit dry-run gap",
        "timeline or run-record entry",
        "findings summary",
        "missing tools or credentials",
        "next cadence entry point"
      ],
      "reliability_promotion_gate": [
        "manual run proves the workflow is useful and bounded before any scheduling proposal",
        "at least one report-only run records inputs, outputs, stop decision and reviewer",
        "owner approval is recorded before enabled=true or external notification/write behavior",
        "rollback or disable path is documented before promotion"
      ],
      "result_policy": "findings go to triage or report artifact; no silent mutation",
      "cleanup_policy": "archive stale runs and do not pin worktrees unless retention is intentional"
    },
    {
      "id": "thread-heartbeat-long-task",
      "owner": "agent-dev-kit",
      "mode": "report-only",
      "enabled_default": false,
      "prompt_requirements": [
        "durable thread wake-up prompt",
        "current thread goal",
        "heartbeat interval",
        "condition for reporting",
        "condition for stopping",
        "manual input trigger"
      ],
      "run_context": "thread",
      "sandbox_policy": "inherits current thread policy; must not widen permissions",
      "approval_policy": "no unattended write unless the active workflow already has explicit approval and rollback evidence",
      "first_run_review": true,
      "first_run_evidence": [
        "heartbeat smoke or explicit dry-run gap",
        "thread timeline entry",
        "changed blocker summary",
        "missing context or manual input",
        "next wake-up or stop decision"
      ],
      "reliability_promotion_gate": [
        "manual heartbeat proves the wake-up prompt preserves context without creating churn",
        "at least one report-only heartbeat records changed evidence or a no-op stop decision",
        "owner approval is recorded before unattended writes or repeated wake-ups",
        "rollback or disable condition and cleanup path are documented before promotion"
      ],
      "result_policy": "preserve context in the current thread and update blockers only when evidence changed",
      "cleanup_policy": "remove heartbeat when stop condition is met"
    }
  ],
  "worktree_contracts": [
    {
      "id": "background-worktree-isolation",
      "owner": "agent-dev-kit",
      "applies_to": [
        "adk-worktree-governance",
        "adk-parallel-agent-governance",
        "automation tasks"
      ],
      "creation_gate": [
        "git repository confirmed",
        "base branch or commit recorded",
        "dirty local changes classified",
        "write scope is disjoint from foreground work"
      ],
      "handoff_gate": [
        "thread id or task id recorded",
        "worktree path recorded",
        "branch checkout limitation acknowledged",
        "validation environment chosen",
        "handoff to Local only after user review"
      ],
      "cleanup_gate": [
        "archive or preserve decision",
        "pinned/permanent status checked",
        "snapshot or diff evidence recorded",
        "no deletion while task is in progress"
      ],
      "must_not": [
        "assume the same branch can be checked out in two worktrees",
        "delete a worktree without preserving review evidence",
        "run background automation against a dirty local checkout without an explicit decision"
      ]
    }
  ],
  "quality_gate": {
    "automations_enabled_default_must_be_false": true,
    "first_runs_require_review": true,
    "first_run_evidence_required": true,
    "manual_reliable_before_scheduling": true,
    "promotion_requires_report_only_history": true,
    "promotion_evidence_required": true,
    "enabled_mode_requires_owner_approval_and_rollback": true,
    "full_access_never_for_default_automation": true,
    "worktree_cleanup_requires_retention_decision": true,
    "risk_fixtures_required": true,
    "dirty_worktree_requires_decision": true,
    "stale_heartbeat_requires_stop_or_replan": true
  },
  "risk_fixtures": [
    {
      "id": "automation-missing-stop-condition",
      "input": "scheduled automation has cadence and prompt but no stop condition",
      "expected": "reject",
      "blocked_gate": "prompt_requirements.stop condition"
    },
    {
      "id": "automation-full-access-unattended",
      "input": "background automation requests danger-full-access with approval_policy=never",
      "expected": "reject",
      "blocked_gate": "quality_gate.full_access_never_for_default_automation"
    },
    {
      "id": "worktree-dirty-local-no-decision",
      "input": "background worktree starts from local checkout with dirty changes but no dirty-state decision",
      "expected": "reject",
      "blocked_gate": "worktree_contracts.creation_gate.dirty local changes classified"
    },
    {
      "id": "thread-heartbeat-stale-no-stop",
      "input": "thread heartbeat has no changed evidence across repeated runs and no stop or replan decision",
      "expected": "needs-fix",
      "blocked_gate": "quality_gate.stale_heartbeat_requires_stop_or_replan"
    }
  ]
}
