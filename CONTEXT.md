# Global Dev Kit 领域语言规范

## 核心概念

### 1. Agent（代理）
**定义**：具有特定角色和职责的AI代理，负责执行特定类型的开发任务。
**特征**：
- 角色明确：每个Agent有明确的职责边界
- 能力专精：专注于特定领域的开发任务
- 协作规范：遵循标准的交接和协作流程

**示例**：
- `requirements-analyst`：需求澄清与验收标准固化
- `architecture-planner`：架构设计与技术选型
- `driver-engineer`：驱动与外设集成实现

### 2. Skill（技能）
**定义**：可复用的开发能力单元，包含具体的开发流程和最佳实践。
**特征**：
- 触发明确：有清晰的触发条件和场景描述
- 流程完整：包含完整的开发流程和验证步骤
- 可组合性：可以与其他技能组合使用

**分类**：
- **核心技能**：基础开发能力，必须具备
- **可选技能**：特定场景能力，按需安装

**示例**：
- `requirements-triage`：需求分类与优先级排序
- `systematic-debugging`：系统化调试方法
- `verification-before-completion`：完成前验证检查

### 3. Profile（配置）
**定义**：针对特定开发场景的技能和Agent组合配置。
**特征**：
- 场景明确：针对特定开发场景优化
- 组合合理：技能和Agent组合经过验证
- 继承清晰：支持配置继承和覆盖

**示例**：
- `core`：通用研发核心配置
- `embedded-fullstack`：嵌入式全栈配置
- `release-hardening`：发布前强化配置

### 4. Workflow（工作流）
**定义**：标准化的开发流程，定义了从需求到交付的完整路径。
**特征**：
- 阶段清晰：包含明确的阶段划分
- 门禁严格：每个阶段有明确的入口/出口条件
- 可追溯：支持变更追溯和验证证据

**标准流程**：
```
propose -> apply -> verify -> review -> archive
```

### 5. Artifact（产物）
**定义**：开发过程中产生的标准化交付物。
**特征**：
- 标签统一：使用标准化的产物标签
- 格式规范：遵循统一的格式要求
- 可验证：支持验证和审查

**产物类型**：
- 需求类：PRD、UserStory
- 设计类：DesignSpec、SystemArch
- 实现类：ImplementationPlan、Code
- 验证类：TestReport、ReviewReport

### 6. Gate（门禁）
**定义**：开发流程中的质量检查点，确保交付物符合标准。
**特征**：
- 条件明确：有清晰的入口/出口条件
- 检查严格：使用标准化的检查方法
- 阻塞机制：不满足条件时阻塞流程

**门禁类型**：
- 入口门禁：检查必需输入是否齐备
- 出口门禁：检查交付物是否完整
- 质量门禁：检查交付物质量是否达标

## 术语表

| 术语 | 英文 | 定义 |
|------|------|------|
| 代理 | Agent | 具有特定角色和职责的AI代理 |
| 技能 | Skill | 可复用的开发能力单元 |
| 配置 | Profile | 针对特定开发场景的技能和Agent组合 |
| 工作流 | Workflow | 标准化的开发流程 |
| 产物 | Artifact | 开发过程中产生的标准化交付物 |
| 门禁 | Gate | 开发流程中的质量检查点 |
| 触发词 | Trigger | 触发技能执行的关键词或条件 |
| 验证证据 | Evidence | 证明交付物符合标准的文档或数据 |
| 变更追溯 | Traceability | 追踪变更历史和影响的能力 |
| 阻塞 | Block | 因条件不满足而暂停流程的状态 |

## 概念关系

### 1. Agent-Skill 关系
- Agent 使用 Skill 执行具体任务
- Skill 定义具体的工作流程和最佳实践
- Agent 根据任务需求选择合适的 Skill

### 2. Profile-Agent-Skill 关系
- Profile 定义了 Agent 和 Skill 的组合配置
- Agent 在 Profile 的指导下使用 Skill
- Profile 确保 Agent 和 Skill 的组合经过验证

### 3. Workflow-Artifact-Gate 关系
- Workflow 定义了 Artifact 的产生流程
- Gate 确保 Artifact 符合质量标准
- Artifact 是 Gate 检查的对象

### 4. 变更追溯关系
- 每个变更都有对应的 Artifact
- Artifact 通过 Gate 验证
- 变更历史通过 Artifact 追溯

## 使用规范

### 1. 术语使用
- 在文档和代码中使用统一的术语
- 避免使用同义词或别名
- 保持术语的一致性

### 2. 概念引用
- 引用概念时使用完整的术语
- 避免使用缩写或简称
- 保持概念的准确性

### 3. 关系描述
- 描述概念关系时使用标准的关系词
- 避免使用模糊的关系描述
- 保持关系的清晰性

## 示例

### 1. Agent 使用示例
```
需求分析师（requirements-analyst）使用需求分类技能（requirements-triage）
对用户需求进行分类和优先级排序，产出需求文档（PRD）。
```

### 2. Workflow 执行示例
```
1. 提出变更（propose）：创建变更提案
2. 实施变更（apply）：执行具体修改
3. 验证变更（verify）：运行测试验证
4. 审查变更（review）：代码审查
5. 归档变更（archive）：保存变更记录
```

### 3. Gate 检查示例
```
入口门禁检查：
- [x] 需求文档（PRD）已齐备
- [x] 设计文档（DesignSpec）已齐备
- [x] 任务拆解（TaskBreakdown）已齐备
- [x] 审批（Approval）已通过

出口门禁检查：
- [x] 代码实现完成
- [x] 测试通过
- [x] 文档更新
- [x] 验证证据齐全
```

## 质量要求

### 1. 术语准确性
- 术语定义必须准确无误
- 术语使用必须符合定义
- 术语更新必须及时同步

### 2. 概念一致性
- 概念定义必须保持一致
- 概念使用必须符合定义
- 概念关系必须清晰明确

### 3. 文档完整性
- 文档必须覆盖所有核心概念
- 文档必须提供使用示例
- 文档必须保持及时更新